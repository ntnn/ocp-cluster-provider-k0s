---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  labels:
    openmcp.cloud/cluster: platform
  name: clusteroptions.k0s.cluster.open-control-plane.io
spec:
  group: k0s.cluster.open-control-plane.io
  names:
    kind: ClusterOptions
    listKind: ClusterOptionsList
    plural: clusteroptions
    singular: clusteroptions
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterOptions carries per-cluster options for a single managed cluster.
          It is cluster-scoped and named after the requesting ControlPlane.
          The k0s provider reads it when creating the backing container.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state.
            properties:
              aliases:
                description: |-
                  Aliases are the fully-qualified hostnames to assign to the cluster's
                  container on the docker network.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
